<project xmlns="http://maven.apache.org/POM/4.0.0" 
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>br.com.rporto.bibliotecadigital</groupId>
  <artifactId>bibliotecadigital</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>bibliotecadigital</name>
  <description>Um projeto que visa desenvolver uma biblioteca e que eu consiga por meus aprendizados em prática.</description>

  <properties>
    <java.version>11</java.version>
  </properties>
  
  <dependencies>
  	<dependency> <groupId>junit</groupId> <!--Tenho que colocar o junit como depêndencia, mas preciso criar a tag dependency para separar as dependencias de forma individual.
  	Só assim o maven vai conseguir interpretar corretamente as dependencias.-->
          <artifactId>junit</artifactId>
          <version>4.13.2</version>
          <scope>test</scope>
          </dependency>
  
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.8.1</version>
        <configuration>
          <source>${java.version}</source>
          <target>${java.version}</target>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
